# Optional APR Model Files

Place trained model files here to enable ML-based APR recommendations:

- `xgb_model.joblib`
- `xgb_model_meta.json`

Without these files, the service runs with the built-in rule-based fallback.
